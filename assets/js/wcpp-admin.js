/**
 * WC Personalisation Panel — Admin JS
 * Settings page + product meta box enhancements.
 * Loaded on admin screens only.
 */
(function ($) {
	'use strict';

	$(document).ready(function () {
		// TODO Phase 1: add any dynamic admin UI (colour preview, repeater fields, etc.)
	});

}(jQuery));
